namespace Domain.Domain {
    public enum GameCategory {
        COUNTRY,
        OBJECT,
        NAME,
        LAST_NAME,
        COLOR,
        CITY,
        FRUIT_VEGETABLE,
        ANIMAL
    }
}
